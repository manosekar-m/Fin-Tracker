name: fin_tracker
description: A complete personal finance tracker app
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  fl_chart: ^0.65.0
  intl: ^0.18.0
  local_auth: ^2.1.6
  uuid: ^4.2.2

flutter:
  uses-material-design: true
